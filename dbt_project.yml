name: 'nyc_analytics'
version: '1.0.0'
config-version: 2
# Some default settings for dbt
profile: 'default'

model-paths: ["group5_models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
 - "target"
 - "dbt_packages"

# Set up models, which are basically SQL files, to create tables
models:
 nyc_analytics:
   +materialized: table

   assignment_work:
     staging:
       +materialized: table
       +schema: 311nyc_Dog_Complaints_staging

     marts:
       +materialized: table
       +schema: 311nyc_Dog_Complaints_staging
      
       shared:
         +materialized: table

       nyc_dog_licensing:
         +materialized: table

       311nyc_Dog_Complaints:
         +materialized: table